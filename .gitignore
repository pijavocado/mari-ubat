node_modules/
.next/
out/
data/
.env
.env*.local
*.log
*.tsbuildinfo
.claude/

# Original source assets — already copied into public/images/, which is
# what the app actually serves. Kept locally, not duplicated in git.
/image/
